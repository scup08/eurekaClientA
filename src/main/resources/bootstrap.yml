eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: eurekaClientA
  cloud:
    config:
      profile: gs
      discovery:
        enabled: true
        service-id: configservice
  
